# Friendy - Android приложение для чата с ИИ

Приложение для Android на Kotlin с чат-интерфейсом, похожим на WhatsApp, для общения с искусственным интеллектом.

## 🚀 Быстрая настройка API

**Для работы чата необходимо настроить API в одном файле:**

📁 **`app/src/main/java/com/friendy/api/ApiConfig.kt`**

Замените значения на строках:
- **Строка 24**: `API_KEY = "your_api_key_here"` → ваш API ключ
- **Строка 27**: `CLIENT_ID = "your_client_id_here"` → ваш Client ID  
- **Строка 30**: `API_ENDPOINT = "https://api.example.com/v1/chat"` → URL вашего API

Подробные инструкции ниже ⬇️

## Возможности

- 💬 Чат-интерфейс в стиле WhatsApp
- 🎨 Три темы оформления (темно-синяя, темная, светлая)
- 📱 Боковое меню с историей чатов по датам
- 💾 Локальное сохранение всех переписок
- ⚙️ Настройки с Пользовательским Соглашением и Политикой Конфиденциальности
- 🔄 Поддержка отправки сообщений по Enter

## Требования

- Android Studio
- Android SDK 29+ (Android 10+)
- Kotlin
- Интернет-соединение для работы с API

## Настройка API

Для работы чата необходимо настроить подключение к API искусственного интеллекта.

### Шаг 1: Откройте файл конфигурации API

Откройте файл: **`app/src/main/java/com/friendy/api/ApiConfig.kt`**

### Шаг 2: Замените параметры API

В файле `ApiConfig.kt` найдите следующие строки и замените значения:

```kotlin
object ApiConfig {
    // Строка 24: Замените на ваш API ключ
    const val API_KEY = "your_api_key_here"
    
    // Строка 27: Замените на ваш Client ID
    const val CLIENT_ID = "your_client_id_here"
    
    // Строка 30: Замените на URL вашего API эндпоинта
    const val API_ENDPOINT = "https://api.example.com/v1/chat"
}
```

### Пример настройки:

```kotlin
object ApiConfig {
    const val API_KEY = "sk-1234567890abcdefghijklmnopqrstuvwxyz"
    const val CLIENT_ID = "client_12345"
    const val API_ENDPOINT = "https://api.openai.com/v1/chat/completions"
}
```

### Шаг 3: Настройка формата запроса (при необходимости)

Если ваш API использует другой формат запроса, отредактируйте следующие файлы:

**`app/src/main/java/com/friendy/api/model/ChatRequest.kt`** (строки 4-6)
```kotlin
data class ChatRequest(
    val message: String,
    val clientId: String
)
```

**`app/src/main/java/com/friendy/api/model/ChatResponse.kt`** (строки 4-6)
```kotlin
data class ChatResponse(
    val response: String
)
```

**`app/src/main/java/com/friendy/api/ApiService.kt`** (строки 14-19)
```kotlin
@POST("chat")
suspend fun sendMessage(
    @Header("Authorization") apiKey: String,
    @Body request: ChatRequest
): ChatResponse
```

### Шаг 4: Настройка заголовков авторизации (при необходимости)

Если ваш API использует другой формат авторизации, отредактируйте файл:

**`app/src/main/java/com/friendy/repository/ChatRepository.kt`** (строка 44)

Текущий формат (строка 44):
```kotlin
apiKey = "Bearer ${ApiConfig.API_KEY}",
```

Если ваш API не использует "Bearer", измените на:
```kotlin
apiKey = ApiConfig.API_KEY  // или другой формат, например "Api-Key ${ApiConfig.API_KEY}"
```

## Структура проекта

```
app/src/main/java/com/friendy/
├── api/
│   ├── ApiConfig.kt          ← ⭐ НАСТРОЙКА API (строки 24, 27, 30)
│   ├── ApiService.kt          ← Интерфейс API (строки 14-19)
│   └── model/
│       ├── ChatRequest.kt     ← Формат запроса (строки 4-6)
│       └── ChatResponse.kt    ← Формат ответа (строки 4-6)
├── data/
│   ├── PreferencesManager.kt  ← Управление настройками
│   ├── database/
│   │   └── AppDatabase.kt     ← База данных Room
│   ├── dao/
│   │   └── MessageDao.kt     ← DAO для сообщений
│   └── model/
│       └── Message.kt         ← Модель сообщения
├── repository/
│   └── ChatRepository.kt      ← Репозиторий (строка 44 - авторизация)
├── ui/
│   ├── MainScreen.kt          ← Главный экран
│   ├── chat/
│   │   └── ChatScreen.kt    ← Экран чата
│   ├── history/
│   │   └── HistoryScreen.kt ← Экран истории
│   ├── settings/
│   │   ├── SettingsScreen.kt ← Экран настроек
│   │   ├── TermsScreen.kt    ← Пользовательское соглашение
│   │   └── PrivacyScreen.kt  ← Политика конфиденциальности
│   └── theme/
│       ├── Color.kt          ← Цвета тем
│       ├── Theme.kt          ← Темизация
│       └── Type.kt           ← Типографика
└── MainActivity.kt            ← Главная активность
```

## Быстрый старт

1. **Клонируйте репозиторий** или откройте проект в Android Studio

2. **Настройте API** в файле `app/src/main/java/com/friendy/api/ApiConfig.kt`

3. **Синхронизируйте Gradle** (File → Sync Project with Gradle Files)

4. **Запустите приложение** на эмуляторе или реальном устройстве

## Использование

### Чат
- Введите сообщение в поле ввода внизу экрана
- Нажмите кнопку отправки или Enter для отправки
- Сообщения пользователя отображаются справа, ответы ИИ - слева

### История чатов
- Нажмите кнопку меню (☰) в левом верхнем углу или свайпните слева направо
- Выберите дату для просмотра переписки за этот день
- Нажмите иконку удаления (🗑️) для удаления переписки за дату

### Настройки
- Нажмите кнопку настроек (⚙️) в правом верхнем углу
- Выберите тему приложения
- Просмотрите Пользовательское Соглашение и Политику Конфиденциальности

## Технологии

- **Kotlin** - язык программирования
- **Jetpack Compose** - UI фреймворк
- **Room** - локальная база данных
- **Retrofit** - HTTP клиент для API
- **DataStore** - хранение настроек
- **Material Design 3** - дизайн система

## Проверка интернет-соединения

Приложение автоматически проверяет наличие интернет-соединения перед отправкой запросов к API:

- ✅ **Автоматическая проверка**: Перед каждым запросом к API проверяется наличие активного интернет-соединения
- 📶 **Визуальная индикация**: 
  - Иконка Wi-Fi в заголовке чата показывает состояние интернета (зеленая = есть интернет, красная = нет интернета)
  - Баннер с предупреждением появляется при отсутствии интернета
- 💬 **Информативные сообщения**: При отсутствии интернета пользователь получает понятное сообщение вместо ошибки API
- 🔄 **Отслеживание изменений**: Приложение отслеживает изменения состояния сети в реальном времени

## Поддержка

При возникновении проблем с API:

1. Проверьте правильность `API_KEY`, `CLIENT_ID` и `API_ENDPOINT`
2. Убедитесь, что устройство подключено к интернету (проверьте индикатор в заголовке)
3. Проверьте формат запроса и ответа API в логах Android Studio
4. Убедитесь, что API эндпоинт доступен и возвращает данные в ожидаемом формате
5. Проверьте разрешения в `AndroidManifest.xml` (INTERNET и ACCESS_NETWORK_STATE уже добавлены)

## Лицензия

© 2025 Все права защищены

Спасибо, что пользуетесь приложением. Ваша VG.

